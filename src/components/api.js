// import { feature } from "topojson-client";

// export async function fetchWorldMap() {
//   const response = await fetch(
//     "https://github.com/manuelmhtr/countries-and-timezones.git"
//   );
//   const data = await response.json();
//   const worldMap = feature(data, data.objects.countries);
//   const topoJSON = JSON.stringify(worldMap);

//   return topoJSON;
// }
